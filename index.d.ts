/**
 * Generate a random string
 * @category String
 */
declare function randomStr(size?: number, dict?: string): string

declare function add(one: number, other: number): number
declare function sub(one: number, other: number): number
declare function mul2(one: number, another: number): number | undefined

export { add, mul2, randomStr, sub }
